name: test
on: [push]

jobs:
  test:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - uses: ./
      id: dl-mingw
    - run: ${{ steps.dl-mingw.outputs.path }}\bin\gcc -v
